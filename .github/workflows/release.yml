name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false
      - uses: pnpm/action-setup@v6
        with:
          version: 11.20.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: integrations/opencode/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile --ignore-scripts
        working-directory: integrations/opencode
      - run: go test ./...
      - run: go vet ./...
      - run: pnpm test
        working-directory: integrations/opencode
      - run: pnpm typecheck
        working-directory: integrations/opencode
      - name: Validate release tag
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! "${GITHUB_REF_NAME}" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z]+([.-][0-9A-Za-z]+)*)?$ ]]; then
            echo "Release tags must be SemVer without build metadata, for example v0.1.0 or v0.2.0-alpha.1." >&2
            exit 1
          fi
          git merge-base --is-ancestor "${GITHUB_SHA}" refs/remotes/origin/main || {
            echo "Release tag must point to a commit reachable from origin/main." >&2
            exit 1
          }
      - name: Build release archives
        shell: bash
        env:
          CGO_ENABLED: "0"
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME}"
          build_date="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          mkdir -p dist
          for target in windows/amd64 windows/arm64 darwin/amd64 darwin/arm64 linux/amd64 linux/arm64; do
            goos="${target%/*}"
            goarch="${target#*/}"
            name="agentmem_${version}_${goos}_${goarch}"
            directory="dist/${name}"
            mkdir -p "${directory}"
            binary="agentmem"
            if [ "${goos}" = "windows" ]; then
              binary="agentmem.exe"
            fi
            GOOS="${goos}" GOARCH="${goarch}" go build \
              -trimpath -buildvcs=false \
              -ldflags "-s -w -X main.version=${version} -X main.commit=${GITHUB_SHA} -X main.buildDate=${build_date}" \
              -o "${directory}/${binary}" ./cmd/agentmem
            cp LICENSE README.md "${directory}/"
            git archive HEAD docs schemas integrations scripts | tar -x -C "${directory}"
            for capability in adapters/*/CAPABILITIES.md; do
              install -D "${capability}" "${directory}/${capability}"
            done
            if [ "${goos}" = "windows" ]; then
              (cd "${directory}" && zip -q -r "../${name}.zip" .)
            else
              tar -C "${directory}" -czf "dist/${name}.tar.gz" .
            fi
          done
          (cd dist && sha256sum ./*.zip ./*.tar.gz > SHA256SUMS)
      - name: Publish GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          args=(--verify-tag --generate-notes --title "${GITHUB_REF_NAME}")
          if [[ "${GITHUB_REF_NAME}" == *-* ]]; then
            args+=(--prerelease)
          fi
          gh release create "${GITHUB_REF_NAME}" "${args[@]}" dist/SHA256SUMS dist/*.zip dist/*.tar.gz
